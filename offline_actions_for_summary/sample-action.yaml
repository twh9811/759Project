name: "Sample Docker Action"

inputs:
  username:
    description: "The username input"
    required: true

  password:
    description: "The password input"
    required: true

outputs:
  token:
    description: "Testing token"

runs:
  using: "docker"
  image: "docker://mydocker/action"
